// test the most basic features:
// - improved includes
// - if without parentheses

#include <stdio>

int main(int argc, char **argv) {
	if argc > 1 {
		printf("too many arguments: %d!\n", argc - 1);
		return 1;
	}

	while argc && argv[--argc] {
		printf("arg: %s\n", argv[argc]);
	}

	if x == 3:
		abc

	printf("correct amount of arguments!\n");

	return 0;
}

